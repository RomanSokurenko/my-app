const Base_URL = 'https://5f7998dbe402340016f9321f.mockapi.io/jobs';

class Api {
   constructor(baseUrl, endpoind) {
      this.baseUrl = baseUrl;
      this.endpoind = endpoind;

   }
   async fetch() { };
}